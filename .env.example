# RailShift APAC — environment variables

# Gemini API key (optional — app works without it, Brief page uses saved fallbacks)
# Get a free key at: https://aistudio.google.com/app/apikey
# IMPORTANT: never use a NEXT_PUBLIC_ prefix — this must stay server-side only
GEMINI_API_KEY=
